RewriteEngine On
RewriteBase /
RewriteRule ^index\.html$ - [L]

# Serve existing files and directories directly (including /v1/)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback for SPA routes
RewriteRule . /index.html [L]
